# Diário — 20/03/2026 (Flush Pré-Compactação — Grupo Automações 15h01 BRT)

---

## CONTEXTO DA SESSÃO

- **Grupo:** Automações (chat_id: -5138173805)
- **Horário:** 15h01 BRT (18h01 UTC)
- **Solicitação:** Dr. Henrique executou protocolo formal de auditoria de memória (4 passos)

---

## DECISÕES / AÇÕES DESTA SESSÃO

### Protocolo de Auditoria de Memória — Iniciado (Passo 1 executado)
- Dr. Henrique solicitou protocolo completo de 4 passos:
  1. Mostrar estado atual da memória → **EXECUTADO**
  2. Aguardar confirmação → **PENDENTE** (sessão compactou antes de confirmação)
  3. Reestruturar MEMORY.md com padrão aprovado → **NÃO EXECUTADO**
  4. Protocolo permanente de boot → **NÃO EXECUTADO**
- **IMPORTANTE:** Passo 1 foi apresentado, Dr. Henrique ainda não confirmou/corrigiu
- Retomar do Passo 2 na próxima sessão

### Arquivos Lidos Nesta Sessão
- `memory/lessons.md` — lido completo
- `memory/pending.md` — lido completo

---

## STATE SNAPSHOT — O QUE ESTÁ ATIVO/QUEBRADO

### N8N — Status dos Workflows
- **Total:** 24 workflows | 20 ativos | 4 inativos
- **F10 (`ppws3IRJo8K6QQJd`) — QUEBRADO** → bug `//` → `??` em "Buscar Task no ClickUp por Signer" — contrato assinados não passam pelo pipeline ADVbox/Asaas
- **F19 (`VtzjQ0StPV8iJcWd`) — BLOQUEADO** → criado via API, webhook não registra sem restart N8N; senha ADVbox hardcoded errada
- **F6 (`QULGGmXizKkgdwnm`) — ESQUELETO** → só Webhook + Resposta, não faz nada ainda
- **Pipeline Asaas (`OwXrNkgiCqRykq7O`) — TOKEN HARDCODED ANTIGO** → token `pk_60972410_2NEHDF941LOLSWCO14C4Q0L5MRMBEOYL` (revogado) em 2 nós; pipeline quebrado para TODOS os clientes
- **GCLID Workflows — INATIVOS** → atribuição Google Ads possivelmente quebrada
- **ZapSign Análise Contratos — INATIVO**
- **WF-SUPPORT (`wXPbGYVJb7pbiaSn`) — ATIVO** → configurado 19/03, whitelist: 5585991436886 + 5585994502609; pendente: teste real do Dr. Henrique + número Ingrid para whitelist

### Evolution API
- Container: `evolution-api` | Imagem: `atendai/evolution-api:v1.8.7`
- Instância: `suporte-ha` | Status: `open` ✅
- Acesso OpenClaw: `http://evolution-api:8080` | API Key: `henry-evolution-2026`

### Google Drive OAuth
- Status: **CRÍTICO** — GCP em Testing mode, token expira a cada ~7 dias
- Última reconexão: 18/03/2026
- **Próxima expiração estimada: ~25/03/2026**
- Ação: Dr. Henrique migrar para Production mode no Google Cloud Console

### Crons — Status
- 9 crons configurados
- Fix de delivery aplicado em 20/03: todos com `delivery.mode: "none"` + agent envia via `message` tool
- Modelos: Sonnet para todos (Haiku teve erros de model string)

### ClickUp API Token
- Token ativo: `pk_60972410_18YZ08VGC8Q1W14SYJ7XYRJ20VH1RZ4W`
- 1Password (UUID: `mth7e2mb6nkrsk2bbty3e4iuli`): **DESATUALIZADO** — Dr. Henrique precisa atualizar manualmente (service account é read-only)

---

## PENDÊNCIAS ABERTAS — PRIORIDADE ALTA

### 🔴 Requerem Dr. Henrique (ação manual)
1. **Pipeline Asaas N8N** — workflow `OwXrNkgiCqRykq7O` → abrir no painel N8N → substituir token hardcoded nos nós "MF01 | Extrair Dados" e "MF01 | Identificar Contrato" pelo token válido `pk_60972410_18YZ08VGC8Q1W14SYJ7XYRJ20VH1RZ4W`
2. **ClickUp 1Password** — item `mth7e2mb6nkrsk2bbty3e4iuli` → atualizar campo password com token atual
3. **ADVbox → 1Password** — salvar credencial (email: `adv.henriqueaugusto@gmail.com` + senha) → desbloqueia F14/F19/onboarding automático
4. **GCP Production Mode** — Google Cloud Console → OAuth consent screen → Publicar app (expira ~25/03)

### 🔴 Técnicas (Henry executa após autorização)
5. **Fix F10** — corrigir bug `//` → `??` + executar onboarding manual dos contratos acumulados desde 12/03
6. **Boot estrutural dos grupos** — criar proposta formal de systemEvent/instrução de boot por grupo → apresentar ao Dr. Henrique
7. **F6 Upload Drive** — implementar: Webhook → Download ZapSign PDF → Upload Google Drive
8. **GABRIEL LINHARES (868hvjr0v)** — ADVbox: F14→F15→F16 (não cadastrado)
9. **ALAN QUESTER** — deletar customer ADVbox duplicado 14311233 + processo 13651174 + 3 tasks erradas

### 🟡 Pendentes de Confirmação
10. **Dashboard "Objetivos Diários" ClickUp** — aguarda 2 confirmações: (a) meta 3 contratos = dia corrido ou úteis? (b) como registrar reuniões realizadas?
11. **WF-SUPPORT** — Dr. Henrique testar do número pessoal e confirmar funcionamento
12. **Asaas 3 clientes** — aguardam "CONFIRMO A EXECUÇÃO": ADAILDO (3x R$1.299, 1ª vencida 16/03), MARCIO LINO (3x, parcela 17/03 vencida), NAIRTON (decidir plano)
13. **6 tasks Central do Cliente Março** — aguardam confirmação para criação

---

## LIÇÕES APLICÁVEIS A ESTE GRUPO (CONSOLIDADO)

### N8N Code Nodes — REGRAS DEFINITIVAS
- `fetch()` → NÃO funciona → usar HTTP Request node
- `$helpers.httpRequest()` → NÃO funciona → usar HTTP Request node
- `$env.QUALQUER` → bloqueado por design → usar credenciais no N8N ou header direto
- `$input` → referencia SEMPRE o nó imediatamente anterior
- `$('Nome do Nó').first().json.campo` → única forma segura de referenciar nó específico
- `$input.first()` em múltiplos itens → colapsa pipeline downstream (verificar `$input.all().length` antes)
- Emoji em `{{ }}` → corrompe JSON → usar JSON Builder node
- PUT API N8N → aceita apenas `executionOrder` em `settings` → outros campos = rejeição silenciosa
- Paginação: sempre verificar `nextCursor` → pode haver mais workflows além dos 50 retornados
- Inserir nó intermediário → re-verificar TODOS os nós downstream antes de salvar

### N8N — Ordem de Execução Correta
- Filtrar evento relevante → DEPOIS buscar dados adicionais (ClickUp, ADVbox) → processar
- NUNCA buscar dados antes de confirmar que o evento é relevante

### Evolution API v1.8.7
- Event names: lowercase com ponto → `messages.upsert` (não `MESSAGES_UPSERT`)
- Conversão: `event.replace(/\./g, '_').toUpperCase()`
- Múltiplos eventos por mensagem → filtrar APENAS `MESSAGES_UPSERT` para processar mensagens novas
- Monitoramento NÃO deve acionar agente diretamente → dupla chamada

### Pipeline / Integrações
- MF02 = texto com labels (`CPF: xxx`, `Valor Total: xxx`, `Vencimento: xxx`) → NUNCA JSON
- OpenAI Responses API → sempre strip de code fences antes de `JSON.parse()`
- Shell com emoji/acentos em variável → usar Python com `PYTHONIOENCODING=utf-8`
- Nginx proxy reverso → resolve comunicação cross-container N8N ↔ Evolution API
- ClickUp field IDs → typo retorna null silenciosamente → sempre copiar da API

### Infraestrutura / Gateway
- SIGUSR1 → reverte mudanças em disco → sempre `docker restart` após editar JSON
- `config.patch` → método correto para alterar configuração (atualiza memória E disco)
- Commit incremental → não esperar solicitação → após cada bloco significativo

---

## PROTOCOLO PENDENTE (RETOMAR NA PRÓXIMA SESSÃO)

O protocolo de auditoria de memória estava no **Passo 2** quando a sessão compactou:
- Passo 1 ✅ Executado — estado apresentado
- Passo 2 ⏳ PENDENTE — aguardando confirmação do Dr. Henrique
- Passo 3 ⏳ PENDENTE — reestruturar MEMORY.md com padrão aprovado
- Passo 4 ⏳ PENDENTE — formalizar protocolo permanente

**Ao retomar:** perguntar ao Dr. Henrique se o estado apresentado no Passo 1 estava correto ou se há correções antes de avançar para o Passo 3.

---

## REGRAS PERMANENTES RECONFIRMADAS NESTA SESSÃO

- **Horário:** SEMPRE BRT (UTC-3). NUNCA UTC nas respostas.
- **Boot de grupo:** carregar `memory/automacoes/PROTOCOLS.md` ANTES de qualquer resposta neste grupo
- **Slack Comercial:** somente leitura — NUNCA postar automaticamente
- **Asaas writes:** exige frase exata "CONFIRMO A EXECUÇÃO"
- **Openclaw.json:** NUNCA editar diretamente
- **Sub-agents:** nunca "fire and forget" — sempre follow-up

---

*Flush criado em: 20/03/2026 16h01 BRT*
*Próxima ação: retomar protocolo auditoria memória — Passo 2*
